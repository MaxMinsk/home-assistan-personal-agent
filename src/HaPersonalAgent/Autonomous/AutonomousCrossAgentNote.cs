namespace HaPersonalAgent.Autonomous;

/// <summary>
/// Что: краткая сводка ДРУГОГО автономного агента для кросс-контекста (HPA-039, часть A).
/// Зачем: агенты не должны быть изолированными силосами — с явного согласия владельца прогон видит, над чем работают
/// и что нашли соседние агенты, чтобы заметить связь. Но только по факту: выдумывать связи запрещено (правило заземления).
/// Как: имя агента + последняя сводка его завершённого прогона + текущий фокус; собирается бюджетно (ограниченное число, усечённо).
/// </summary>
public sealed record AutonomousCrossAgentNote(
    string AgentName,
    string Summary,
    string? Focus);
